# Supabase (local defaults — replace with cloud values for production)
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-anon-key

# Cloud project (scholarflow): https://qeqjxtcvutbpraxdztgu.supabase.co
# Copy anon key from Supabase Dashboard → Project Settings → API

# CLI / cloud scripts only (never commit real tokens)
# SUPABASE_ACCESS_TOKEN=sbp_...
# SUPABASE_DASHBOARD_TOKEN=  # optional; dashboard session JWT for one-off seeding
